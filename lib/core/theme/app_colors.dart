import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF0F4C3A);
  static const primaryLight = Color(0xFF1A7556);
  static const primaryDark = Color(0xFF0A3528);

  // Accent
  static const accent = Color(0xFFF4B942);
  static const accentSoft = Color(0xFFFFF4D9);
  static const accentDark = Color(0xFFE89B25);

  // Background
  static const background = Color(0xFFF5F1EA);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF1A2421);
  static const textSecondary = Color(0xFF6B7570);
  static const textLight = Color(0xFF9CA59F);

  // Status
  static const success = Color(0xFF10B981);
  static const danger = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
  static const info = Color(0xFF3B82F6);

  // Border
  static const border = Color(0xFFE8E4DC);
}
