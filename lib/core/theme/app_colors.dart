import 'package:flutter/material.dart';

class AppColors {
  // Primary — Blue from Stitch "Jordanian Commuter Harmony"
  static const primary = Color(0xFF005EA3);
  static const primaryLight = Color(0xFF0277CC);
  static const primaryDark = Color(0xFF00487F);

  // Accent / Secondary — warm gold
  static const accent = Color(0xFFFEAE2C);
  static const accentSoft = Color(0xFFFFDDB4);
  static const accentLight = Color(0xFFFFF8F0);
  static const accentDark = Color(0xFF835500);

  // Background & Surfaces — light blue-white tint (Stitch)
  static const background = Color(0xFFF8F9FF);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceContainer = Color(0xFFEBEEF5);
  static const surfaceContainerHigh = Color(0xFFE6E8F0);
  static const surfaceContainerLow = Color(0xFFF1F3FB);

  // Text — cool blue-charcoal
  static const textPrimary = Color(0xFF181C21);
  static const textSecondary = Color(0xFF404752);
  static const textLight = Color(0xFF717783);

  // Status
  static const success = Color(0xFF4CAF50);
  static const danger = Color(0xFFBA1A1A);
  static const warning = Color(0xFFF5A623);
  static const info = Color(0xFF0277CC);

  // Border / Outline
  static const border = Color(0xFFC0C7D3);

  // Extended palette
  static const primarySurface = Color(0xFFD2E4FF);
  static const shimmer = Color(0xFFF1F3FB);
  static const shadowColor = Color(0x1A1B7FD4);
  static const gradientStart = Color(0xFF005EA3);
  static const gradientEnd = Color(0xFF0277CC);

  // Stitch extras
  static const onPrimaryContainer = Color(0xFFA1C9FF);
  static const inverseSurface = Color(0xFF2D3136);
  static const inverseOnSurface = Color(0xFFEEF0F8);
  static const inversePrimary = Color(0xFFA1C9FF);
  static const secondaryContainer = Color(0xFFFEAE2C);
  static const onSecondaryContainer = Color(0xFF6B4500);
}
